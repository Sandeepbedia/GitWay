package com.io.git.way.ui.theme

/**
 * User-selectable theme options, per PRD Theme System:
 * System Default, Light, Dark, AMOLED Dark.
 */
enum class AppThemeMode {
    SYSTEM,
    LIGHT,
    DARK,
    AMOLED
}
